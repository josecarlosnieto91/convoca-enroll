{
    "name": "convoca/enroll",
    "license": "GPL-2.0-or-later",
    "description": "Plugin de inscripcion para Convoca",
    "type": "wordpress-plugin",
    "require": {
        "php": ">=8.1",
        "chillerlan/php-qrcode": "^6.0",
        "eluceo/ical": "^2.9"
    },
    "autoload": {
        "psr-4": {
            "Convoca\\Enroll\\": "includes/"
        },
        "classmap": [
            "admin/",
            "media/",
            "social/",
            "public/"
        ]
    },
    "config": {
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true,
            "phpstan/extension-installer": true
        }
    },
    "require-dev": {
        "phpstan/phpstan": "^2.2",
        "dealerdirect/phpcodesniffer-composer-installer": "^1.0",
        "wp-coding-standards/wpcs": "^3.4",
        "php-stubs/wordpress-stubs": "^7.0",
        "phpunit/phpunit": "^10.5",
        "phpstan/extension-installer": "^1.4"
    },
    "scripts": {
        "phpcs": "phpcs --standard=.phpcs.xml.dist --runtime-set ignore_warnings_on_exit 1",
        "phpcbf": "phpcbf --standard=.phpcs.xml.dist",
        "phpstan": "phpstan analyse --configuration=phpstan.neon.dist --memory-limit=4G",
        "phpunit": "phpunit",
        "test": [
            "@phpcs",
            "@phpstan",
            "@phpunit"
        ]
    }
}
